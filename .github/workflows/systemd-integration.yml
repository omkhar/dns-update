---
name: Systemd Integration

'on':
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: '47 3 * * *'

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  systemd:
    name: ${{ matrix.name }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Debian Stable
            label: debian-stable
            image: "debian:stable-slim@sha256:\
              99fc6d2a0882fcbcdc452948d2d54eab91faafc7db037df82425edcdcf950e1f"
            family: debian
          - name: Debian Unstable
            label: debian-unstable
            image: "debian:unstable-slim@sha256:\
              4629867cb5aa1f25958b91d0ac0a867308ec5a72d470811fc2e551df1e240087"
            family: debian
          - name: Ubuntu Stable
            label: ubuntu-stable
            image: "ubuntu:rolling@sha256:\
              4a9232cc47bf99defcc8860ef6222c99773330367fcecbf21ba2edb0b810a31e"
            family: ubuntu
          - name: Ubuntu Unstable
            label: ubuntu-unstable
            image: "ubuntu:devel@sha256:\
              91832dcd7bc5e44c098ecefc0a251a5c5d596dae494b33fb248e01b6840f8ce0"
            family: ubuntu
          - name: Fedora Stable
            label: fedora-stable
            image: "quay.io/fedora/fedora:43@sha256:\
              57b692a43ab02f16b5fa814f0ff2a3ebdc30ee0245d8ef50fdc17bd2e47ecbff"
            family: fedora
          - name: Fedora Unstable
            label: fedora-unstable
            image: "quay.io/fedora/fedora:rawhide@sha256:\
              574edacc07a5bc212fc12e3685b2ae0a0124bd774a99ed4701f2ac00dbe596a3"
            family: fedora
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@4b73464bb391d4059bd26b0524d20df3927bd417
        with:
          go-version-file: go.mod

      - name: Install packaging tools
        shell: bash
        run: |
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends rpm

      - name: Run systemd timer integration test
        env:
          PACKAGING_SYSTEMD_TEST_MODE: package
          TEST_FAMILY: ${{ matrix.family }}
          TEST_IMAGE: ${{ matrix.image }}
          TEST_LABEL: ${{ matrix.label }}
        run: |
          set -euo pipefail
          case "${TEST_LABEL}" in
            debian-stable|\
            debian-unstable|\
            ubuntu-stable|\
            ubuntu-unstable|\
            fedora-stable|\
            fedora-unstable)
              ;;
            *)
              echo "Unexpected systemd test label: ${TEST_LABEL}" >&2
              exit 1
              ;;
          esac

          case "${TEST_IMAGE}" in
            "debian:stable-slim@sha256:\
            99fc6d2a0882fcbcdc452948d2d54eab91faafc7db037df82425edcdcf950e1f"|\
            "debian:unstable-slim@sha256:\
            4629867cb5aa1f25958b91d0ac0a867308ec5a72d470811fc2e551df1e240087"|\
            "ubuntu:rolling@sha256:\
            4a9232cc47bf99defcc8860ef6222c99773330367fcecbf21ba2edb0b810a31e"|\
            "ubuntu:devel@sha256:\
            91832dcd7bc5e44c098ecefc0a251a5c5d596dae494b33fb248e01b6840f8ce0"|\
            "quay.io/fedora/fedora:43@sha256:\
            57b692a43ab02f16b5fa814f0ff2a3ebdc30ee0245d8ef50fdc17bd2e47ecbff"|\
            "quay.io/fedora/fedora:rawhide@sha256:\
            574edacc07a5bc212fc12e3685b2ae0a0124bd774a99ed4701f2ac00dbe596a3")
              ;;
            *)
              echo "Unexpected systemd test image: ${TEST_IMAGE}" >&2
              exit 1
              ;;
          esac

          case "${TEST_FAMILY}" in
            debian|ubuntu|fedora) ;;
            *)
              echo "Unexpected systemd test family: ${TEST_FAMILY}" >&2
              exit 1
              ;;
          esac

          chmod +x packaging/test-systemd-timer.sh
          ./packaging/test-systemd-timer.sh \
            --label "${TEST_LABEL}" \
            --image "${TEST_IMAGE}" \
            --family "${TEST_FAMILY}"
