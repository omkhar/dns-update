# Optional runtime overrides for dns-update.service.
# Install this file at /etc/dns-update/dns-update.env and uncomment only what
# you need.

# DNS_UPDATE_TIMEOUT=2m
# DNS_UPDATE_VERBOSE=true
# DNS_UPDATE_DRY_RUN=false

# Optional config path override:
# DNS_UPDATE_CONFIG=/etc/dns-update/config.json
