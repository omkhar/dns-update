// Package config loads, merges, and validates runtime configuration.
//
// It accepts settings from config files, environment variables, and command
// line overrides, then normalizes them into validated values used by the app.
package config
